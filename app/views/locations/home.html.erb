<main class="">
    <section class="bg-black flex flex-col justify-end pb-2">
        <%= render "locations/home_header"%>
    </section>
    <section class="bg-slate-900 flex flex-col justify-center px-8 p-8 md:h-screen md:px-16">
        <%= render "locations/home_map"%>
    </section>
    <section class="bg-slate-800 flex flex-col justify-center px-8 p-8 md:h-screen md:px-16">
        <%= render "locations/home_timeline"%>
    </section>
    <section class="">
        <%= render "locations/home_footer"%>
    </section>
</main>
